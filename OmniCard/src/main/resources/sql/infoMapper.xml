<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "http://mybatis.org/dtd/mybatis-3-mapper.dtd" "-//mybatis.org//DTD Mapper 3.0//EN">
<mapper namespace="info">
<select id="getMyInfo" parameterType="String" resultType="command.MemberCommand">
	select * from member where id=#{id}
</select>

<update id="infoupdate" parameterType="map">
	update member set name=#{command.name},pw=#{command.pw},
	addr1=#{command.addr1},addr2=#{command.addr2},addr3=#{command.addr3},
	zip_code1=#{command.zip_code1},zip_code2=#{command.zip_code2},
	cel_tel=#{command.cel_tel},com_tel=#{command.com_tel},
	email=#{email} where id=#{id}
</update>

</mapper>